cores = sysinfo(17) - 1
if cores <= 1{ 
	DEFAULT_CORE_NUM = 1
}else: if cores <= 4 : DEFAULT_CORE_NUM = cores - 1
}else: if cores <= 8 : DEFAULT_CORE_NUM = cores - 2
}else: if cores <= 16: DEFAULT_CORE_NUM = cores - 4
}else:	               DEFAULT_CORE_NUM = cores - 8
