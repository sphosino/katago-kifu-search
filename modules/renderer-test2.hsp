#include "renderer.hsp"

randomize
buffer 3, 64, 64 // 仮のスプライト用画像
color 255, 0, 0: boxf 0, 0, 64, 64

gsel 0

make_draw_object TYPE_SPRITE, strf("3,0,0,64,64,%d,%d,1.000,0,128", rnd(400), rnd(400)), 240
make_draw_object TYPE_SPRITE, strf("3,0,0,64,64,%d,%d,1.000,0,128", rnd(400), rnd(400)), 240
*main
	color:boxf:color 240,240,240
	draw_objects

	if rnd(10) = 0 {
		
	}
	await 17
	redraw: redraw 2
	goto *main