#include "renderer.hsp"

make_draw_object TYPE_RECT,"0,0,150,150,$00FF00", 60 //１秒
	set_priority_draw_object stat, PRIORITY_FRONT //表に来た！！OK！！
	
make_draw_object TYPE_CIRCLE,"100,100,250,250,$FF0000", 120 //２秒
	// set_priority_draw_object stat, PRIORITY_FRONT //同じ優先度は登録順！

*main
	color:boxf:color 240,240,240
	draw_objects

	if rnd(10) = 0{
		type = rnd(3)
		if type == TYPE_RECT || type == TYPE_CIRCLE{
			make_draw_object type,strf("%d,%d,%d,%d,%d",rnd(400),rnd(400),rnd(400),rnd(400), rnd(256)<<16|rnd(256)<<8|rnd(256)), 120
		}else{
			make_draw_object TYPE_STR, strf("%s,%d,%d,%d,%d,%s","メイリオ",rnd(30) + 20, rnd(400),rnd(400),rnd(256)<<16|rnd(256)<<8|rnd(256),"HELLO"), 120
			set_priority_draw_object stat, PRIORITY_FRONT //文字列は常に前に表示
		}
	}
	await 17
	redraw: redraw 2
	goto*main

