katago棋譜検索システム

棋譜リストフォルダに入っているkatago-rating-gameの中から検索します！

棋譜リストから棋譜を選択すると閲覧できる。

碁盤の上でドラッグすると検索したい範囲を指定できる。（複数組み合わせて指定可能）
右クリックで直前に指定した範囲を取り除きます。

検索ボタンで検索開始！（起動した後の初回は棋譜を読み込むため、時間がかかる）

下のリストボックスに検索結果が表示されるので、クリックして閲覧してください。